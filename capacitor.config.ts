import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bigsbiescreations.ballisticscalculator',
  appName: 'Ballistics Calculator',
  webDir: 'dist'
};

export default config;
